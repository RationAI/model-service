apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.pvcs.huggingface.claimName }}
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.pvcs.huggingface.storage }}
  storageClassName: {{ .Values.pvcs.storageClassName }}
